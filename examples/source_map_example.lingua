// Example demonstrating source map generation
// This file can be compiled with source maps enabled

fn add(a: i32, b: i32): i32 {
    a + b
}

fn _start(): i32 {
    let result: i32 = add(10, 20);
    println(result);
    0
}